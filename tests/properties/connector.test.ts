/**
 * Connector persistence tests have been moved to tests/integration/connector.test.ts
 *
 * Integration tests require Bun's native SQL driver and must be run with:
 *   bun test tests/integration/
 *
 * This file is a placeholder - the actual tests are in the integration directory.
 */

import { describe, it } from 'vitest';

describe('Connector Data Persistence', () => {
	it.skip('tests moved to tests/integration/connector.test.ts (run with: bun test tests/integration/)', () => {
		// See tests/integration/connector.test.ts for actual property tests
		// These require a PostgreSQL database and Bun runtime
	});
});
